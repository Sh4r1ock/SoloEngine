# 运行面板综合修复方案

## 一、设计理念（遵循现有架构）

### 1.1 架构层次

```
前端层 (React + Zustand)
    ├── RunPanel.tsx - 主面板组件（对话栏UI）
    ├── useRunWebSocket.ts - WebSocket连接管理
    ├── runStore.ts - 状态管理（Zustand + persist）
    └── runApi.ts - HTTP API调用

后端层 (FastAPI)
    ├── run.py - 运行API端点
    ├── database.py - 数据模型定义

数据层 (SQLite)
    ├── AgenticFlowSessionModel - 会话元数据
    └── SessionMessageModel - 会话消息
```

### 1.2 设计原则

1. **最小化修改范围** - 仅修改必要的代码
2. **复用现有机制** - 优先使用现有函数和数据
3. **不新增不必要的API** - 能在前端处理的就在前端处理
4. **统一复用逻辑** - 相同功能使用同一个函数

---

## 二、执行内容总览

### 2.1 新增代码

| 文件 | 新增内容 | 位置 |
|------|----------|------|
| `RunPanel.tsx` | `formatSmartTime` 函数（统一时间格式化） | 约第237行 |
| `RunPanel.tsx` | `getStatusIcon` 函数 | 约第237行 |
| `RunPanel.tsx` | `getStatusText` 函数 | 约第237行 |
| `RunPanel.tsx` | `streamingToolCalls` 状态 | 约第156行 |
| `RunPanel.tsx` | Session恢复useEffect | 约第290行 |
| `runStore.ts` | `currentSessionId` 持久化配置 | 第302-305行 |

> **简化说明**：需求4（首个chunk等待状态）复用现有的 `llmLoading` 和 `isRunning` 状态，不需要新增 `waitingForFirstChunk` 状态。

### 2.2 修改代码

| 文件 | 修改内容 | 位置 |
|------|----------|------|
| `RunPanel.tsx` | 流式输出样式统一 | 第2233-2241行 |
| `RunPanel.tsx` | 消息内容样式统一 | 第2155-2162行 |
| `RunPanel.tsx` | 工具调用折叠逻辑 | 第2102行 |
| `RunPanel.tsx` | 面板布局样式 | 第1938-1945行 |
| `RunPanel.tsx` | 首个chunk等待状态 | 第1946-1975行 |
| `RunPanel.tsx` | Session栏按钮文字 | 第1810行 |
| `RunPanel.tsx` | Session栏任务统计 | 第1811行 |
| `RunPanel.tsx` | Session项UI改造 | 第1871-1899行 |
| `RunPanel.tsx` | formatTime函数改为复用formatSmartTime | 第1501行 |
| `useRunWebSocket.ts` | 心跳间隔优化 | 第138行 |
| `useRunWebSocket.ts` | 重连策略优化 | 第272-278行 |
| `useRunWebSocket.ts` | 最大重连次数 | 第107行 |
| `run.py` | Session排序改为created_at | 第563行 |
| `run.py` | 新增first_assistant_content字段 | 第565-584行 |

### 2.3 删除代码

| 文件 | 删除内容 | 位置 |
|------|----------|------|
| 无 | 无 | 无 |

---

## 三、具体执行步骤

### 3.1 第一阶段：流式输出缩进一致性问题

**文件**：`frontend/src/components/RunPanel/RunPanel.tsx`

#### 3.1.1 统一流式输出样式

**位置**：第2233-2241行

```tsx
// 修改前
{streamingContent && (
  <div style={{ 
    whiteSpace: 'pre-wrap', 
    lineHeight: 1.7, 
    fontSize: 14, 
    color: 'var(--text-100)',
  }}>
    {streamingContent}
  </div>
)}

// 修改后
{streamingContent && (
  <div style={{ 
    whiteSpace: 'pre-wrap',
    wordBreak: 'break-word',
    lineHeight: 1.7, 
    fontSize: 14, 
    color: 'var(--text-100)',
  }}>
    {streamingContent}
  </div>
)}
```

#### 3.1.2 统一消息内容样式

**位置**：第2155-2162行

```tsx
// 修改前
<div style={{ 
  whiteSpace: 'pre-wrap', 
  lineHeight: 1.7,
  fontSize: 14,
  color: 'var(--text-100)',
}}>
  {msg.content}
</div>

// 修改后
<div style={{ 
  whiteSpace: 'pre-wrap',
  wordBreak: 'break-word',
  lineHeight: 1.7,
  fontSize: 14,
  color: 'var(--text-100)',
}}>
  {msg.content}
</div>
```

---

### 3.2 第二阶段：工具输出自动展开与折叠功能

**文件**：`frontend/src/components/RunPanel/RunPanel.tsx`

#### 3.2.1 新增流式工具调用状态

**位置**：第156行

```tsx
// 修改前
const [collapsedToolCalls, setCollapsedToolCalls] = useState<Set<string>>(new Set());
const isConnectedRef = useRef(false);

// 修改后
const [collapsedToolCalls, setCollapsedToolCalls] = useState<Set<string>>(new Set());
const [streamingToolCalls, setStreamingToolCalls] = useState<Map<string, boolean>>(new Map());
const isConnectedRef = useRef(false);
```

#### 3.2.2 修改工具调用折叠判断逻辑

**位置**：第2102行

```tsx
// 修改前
{!collapsedToolCalls.has(`${msg.id}-${idx}-${tcIdx}`) && (

// 修改后
{(!collapsedToolCalls.has(`${msg.id}-${idx}-${tcIdx}`) || streamingToolCalls.has(tc.id)) && (
```

---

### 3.3 第三阶段：运行面板布局与滚动条问题

**文件**：`frontend/src/components/RunPanel/RunPanel.tsx`

#### 3.3.1 优化消息列表容器样式

**位置**：第1938-1945行

```tsx
// 修改前
<div style={{ 
  flex: 1, 
  overflow: 'auto', 
  padding: '16px',
  display: 'flex',
  flexDirection: 'column',
  background: 'var(--bg-100)',
}}>

// 修改后
<div style={{ 
  flex: 1, 
  overflow: 'auto', 
  overflowX: 'hidden',
  padding: '16px',
  display: 'flex',
  flexDirection: 'column',
  background: 'var(--bg-100)',
  minHeight: 0,
}}>
```

---

### 3.4 第四阶段：首个chunk等待状态的前端反馈

**文件**：`frontend/src/components/RunPanel/RunPanel.tsx`

**简化分析**：
- 现有代码已有 `llmLoading` 和 `isRunning` 状态
- 等待状态 = `llmLoading || isRunning` 且没有流式内容
- 不需要新增状态，只需修改UI判断条件

#### 3.4.1 修改空状态判断逻辑

**位置**：第1946-1975行

```tsx
// 修改前
{llmMessages.length === 0 && !streamingContent && !streamingThinkingContent ? (
  // 空状态UI
) : (
  // 消息列表
)}

// 修改后 - 复用现有llmLoading和isRunning状态
{llmMessages.length === 0 && !streamingContent && !streamingThinkingContent && !(llmLoading || isRunning) ? (
  // 空状态UI（保持不变）
) : (llmLoading || isRunning) && !streamingContent && !streamingThinkingContent ? (
  // 新增：等待状态UI（复用现有状态，不需要新增状态变量）
  <div style={{ 
    flex: 1,
    display: 'flex', 
    flexDirection: 'column',
    justifyContent: 'center', 
    alignItems: 'center',
    padding: '40px 16px',
  }}>
    <div style={{
      width: 56,
      height: 56,
      borderRadius: '50%',
      background: 'linear-gradient(135deg, var(--primary-100), var(--primary-200))',
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      marginBottom: 20,
    }}>
      <LoadingOutlined style={{ fontSize: 24, color: '#fff' }} />
    </div>
    <Text style={{ fontSize: 14, color: 'var(--text-200)', fontWeight: 500 }}>
      正在思考中...
    </Text>
  </div>
) : (
  // 消息列表（保持不变）
)}
```

**说明**：复用现有的 `llmLoading` 和 `isRunning` 状态，不需要新增 `waitingForFirstChunk` 状态变量。

---

### 3.5 第五阶段：WebSocket连接稳定性问题

**文件**：`frontend/src/hooks/useRunWebSocket.ts`

#### 3.5.1 优化心跳间隔

**位置**：第138行

```tsx
// 修改前
}, 30000);

// 修改后
}, 15000);
```

#### 3.5.2 优化重连策略

**位置**：第272-278行

```tsx
// 修改前
if (autoReconnect && reconnectAttemptsRef.current < maxReconnectAttempts) {
  reconnectAttemptsRef.current++;
  console.log(`Reconnecting... Attempt ${reconnectAttemptsRef.current}/${maxReconnectAttempts}`);
  reconnectTimeoutRef.current = setTimeout(() => {
    connect();
  }, reconnectInterval);
}

// 修改后
if (autoReconnect && reconnectAttemptsRef.current < maxReconnectAttempts) {
  reconnectAttemptsRef.current++;
  const delay = Math.min(reconnectInterval * Math.pow(2, reconnectAttemptsRef.current - 1), 30000);
  console.log(`Reconnecting... Attempt ${reconnectAttemptsRef.current}/${maxReconnectAttempts} in ${delay}ms`);
  reconnectTimeoutRef.current = setTimeout(() => {
    connect();
  }, delay);
}
```

#### 3.5.3 增加最大重连次数

**位置**：第107行

```tsx
// 修改前
maxReconnectAttempts = 5,

// 修改后
maxReconnectAttempts = 10,
```

---

### 3.6 第六阶段：Session栏调整

**文件**：`frontend/src/components/RunPanel/RunPanel.tsx` 和 `backend/app/api/v1/run.py`

#### 3.6.1 后端：调整Session排序规则

**位置**：`run.py` 第563行

```python
# 修改前
sessions = query.order_by(AgenticFlowSessionModel.updated_at.desc()).limit(limit).all()

# 修改后
sessions = query.order_by(AgenticFlowSessionModel.created_at.desc()).limit(limit).all()
```

#### 3.6.2 后端：新增first_assistant_content字段

**位置**：`run.py` 第565-584行

```python
# 修改前
return {
    "code": 200,
    "message": "Sessions retrieved",
    "data": [
        {
            "id": s.id,
            "agentic_flow_id": s.agentic_flow_id,
            "run_project_id": s.run_project_id,
            "status": s.status,
            "error": s.error,
            "token_usage": s.token_usage,
            "started_at": format_datetime(s.started_at),
            "completed_at": format_datetime(s.completed_at),
            "created_at": format_datetime(s.created_at),
            "updated_at": format_datetime(s.updated_at),
            "duration_ms": s.duration_ms
        }
        for s in sessions
    ]
}

# 修改后
result = []
for s in sessions:
    first_assistant_msg = db.query(SessionMessageModel).filter(
        SessionMessageModel.session_id == s.id,
        SessionMessageModel.role == 'assistant'
    ).order_by(SessionMessageModel.message_index).first()
    
    first_assistant_content = None
    if first_assistant_msg and first_assistant_msg.data:
        for block in first_assistant_msg.data:
            if block.get('type') == 'content' and block.get('content'):
                first_assistant_content = block['content'][:30]
                break
    
    result.append({
        "id": s.id,
        "agentic_flow_id": s.agentic_flow_id,
        "run_project_id": s.run_project_id,
        "status": s.status,
        "error": s.error,
        "token_usage": s.token_usage,
        "started_at": format_datetime(s.started_at),
        "completed_at": format_datetime(s.completed_at),
        "created_at": format_datetime(s.created_at),
        "updated_at": format_datetime(s.updated_at),
        "duration_ms": s.duration_ms,
        "first_assistant_content": first_assistant_content,
    })

return {
    "code": 200,
    "message": "Sessions retrieved",
    "data": result
}
```

#### 3.6.3 前端：新增统一时间格式化函数（复用于两处）

**位置**：`RunPanel.tsx` 约第237行（在 `toggleToolCallsCollapse` 函数之后）

```tsx
// 新增：统一的时间格式化函数，复用于Session栏和对话消息
const formatSmartTime = (dateStr?: string) => {
  if (!dateStr) return '';
  const date = new Date(dateStr);
  const now = new Date();
  
  const isSameYear = date.getFullYear() === now.getFullYear();
  const isToday = date.toDateString() === now.toDateString();
  
  const hours = date.getHours().toString().padStart(2, '0');
  const minutes = date.getMinutes().toString().padStart(2, '0');
  const timeStr = `${hours}:${minutes}`;
  
  if (isToday) {
    return timeStr;
  }
  
  const month = date.getMonth() + 1;
  const day = date.getDate();
  
  if (isSameYear) {
    return `${month}月${day}日 ${timeStr}`;
  }
  
  const year = date.getFullYear();
  return `${year}年${month}月${day}日 ${timeStr}`;
};

const getStatusIcon = (status?: string) => {
  switch (status) {
    case 'completed':
      return <span style={{ color: '#52c41a', fontSize: 12 }}>●</span>;
    case 'cancelled':
    case 'interrupted':
      return <span style={{ color: '#faad14', fontSize: 12 }}>●</span>;
    case 'error':
      return <span style={{ color: '#ff4d4f', fontSize: 12 }}>●</span>;
    default:
      return <span style={{ color: '#d9d9d9', fontSize: 12 }}>●</span>;
  }
};

const getStatusText = (status?: string) => {
  switch (status) {
    case 'completed':
      return '任务完成';
    case 'cancelled':
    case 'interrupted':
      return '任务中断';
    case 'error':
      return '任务异常';
    default:
      return '进行中';
  }
};
```

#### 3.6.4 前端：修改formatTime函数复用formatSmartTime

**位置**：`RunPanel.tsx` 约第1501行

```tsx
// 修改前
const formatTime = (dateStr?: string) => {
  if (!dateStr) return '';
  const date = new Date(dateStr);
  const now = new Date();
  const diff = now.getTime() - date.getTime();
  
  if (diff < 60000) return '刚刚';
  if (diff < 3600000) return `${Math.floor(diff / 60000)} 分钟前`;
  if (diff < 86400000) return `${Math.floor(diff / 3600000)} 小时前`;
  if (diff < 604800000) return `${Math.floor(diff / 86400000)} 天前`;
  
  return date.toLocaleDateString('zh-CN');
};

// 修改后 - 复用formatSmartTime
const formatTime = formatSmartTime;
```

#### 3.6.5 前端：修改"新会话"为"新任务"

**位置**：`RunPanel.tsx` 第1810行

```tsx
// 修改前
新会话

// 修改后
新任务
```

#### 3.6.6 前端：新增任务统计UI（复用现有sessions数组）

**位置**：`RunPanel.tsx` 第1811行

```tsx
// 修改前
</Button>
</div>

// 修改后
</Button>

{sessions.length > 0 && (
  <div style={{ 
    marginTop: 8, 
    padding: '8px 12px', 
    background: 'var(--bg-300)', 
    borderRadius: 6,
    display: 'flex',
    justifyContent: 'space-between',
    alignItems: 'center',
  }}>
    <Text style={{ fontSize: 12, color: 'var(--text-300)' }}>任务总数</Text>
    <Text style={{ fontSize: 12, color: 'var(--text-200)', fontWeight: 500 }}>
      {sessions.length}
    </Text>
  </div>
)}
</div>
```

**说明**：直接使用前端已有的 `sessions.length`，不需要新增后端API。

#### 3.6.7 前端：修改Session项UI

**位置**：`RunPanel.tsx` 第1871-1899行

```tsx
// 修改前
<div style={{ overflow: 'hidden', flex: 1 }}>
  <div style={{ 
    fontWeight: currentSessionId === session.id ? 600 : 450,
    fontSize: 13,
    overflow: 'hidden',
    textOverflow: 'ellipsis',
    whiteSpace: 'nowrap',
  }}>
    {session.name}
  </div>
  <div style={{ 
    fontSize: 11, 
    opacity: 0.65, 
    marginTop: 2,
    overflow: 'hidden',
    textOverflow: 'ellipsis',
    whiteSpace: 'nowrap',
  }}>
    {(session.messages && session.messages.length > 0 
      ? (() => {
          const lastMsg = session.messages[session.messages.length - 1];
          const content = lastMsg.data && lastMsg.data[0] && lastMsg.data[0].content 
            ? lastMsg.data[0].content 
            : lastMsg.content || '';
          return content.slice(0, 18) + (content.length > 18 ? '...' : '');
        })()
      : '空对话'
    )}
  </div>
</div>

// 修改后
<div style={{ overflow: 'hidden', flex: 1 }}>
  <div style={{ 
    display: 'flex',
    alignItems: 'center',
    gap: 6,
    marginBottom: 2,
  }}>
    {getStatusIcon(session.status)}
    <span style={{
      fontWeight: currentSessionId === session.id ? 600 : 450,
      fontSize: 13,
      overflow: 'hidden',
      textOverflow: 'ellipsis',
      whiteSpace: 'nowrap',
    }}>
      {session.firstAssistantContent || session.name || `任务 ${session.id.substring(0, 8)}`}
    </span>
  </div>
  <div style={{ 
    fontSize: 11, 
    opacity: 0.65, 
    display: 'flex',
    alignItems: 'center',
    gap: 4,
  }}>
    <span>{getStatusText(session.status)}</span>
    <span>·</span>
    <span>{formatSmartTime(session.createdAt || session.created_at)}</span>
  </div>
</div>
```

**说明**：复用 `formatSmartTime` 函数显示时间。

---

### 3.7 第七阶段：Session创建与管理机制优化

**文件**：`frontend/src/store/runStore.ts` 和 `frontend/src/components/RunPanel/RunPanel.tsx`

#### 3.7.1 修改runStore持久化配置

**位置**：`runStore.ts` 第302-305行

```tsx
// 修改前
{
  name: 'run-store',
  partialize: () => ({}),
}

// 修改后
{
  name: 'run-store',
  partialize: (state) => ({
    currentSessionId: state.currentSessionId,
  }),
}
```

#### 3.7.2 新增页面加载时的Session恢复逻辑

**位置**：`RunPanel.tsx` 约第290行

```tsx
// 新增useEffect
useEffect(() => {
  const restoreSession = async () => {
    if (!agenticFlowId || !currentProject?.id) return;
    
    const storedState = localStorage.getItem('run-store');
    if (storedState) {
      try {
        const parsed = JSON.parse(storedState);
        const storedSessionId = parsed.state?.currentSessionId;
        
        if (storedSessionId) {
          try {
            const messages = await runApi.getSessionMessages(storedSessionId);
            if (messages && messages.length >= 0) {
              setCurrentSessionId(storedSessionId);
              if (messages.length > 0) {
                const llmMsgs: LLMMessage[] = messages.map(msg => ({
                  id: msg.id,
                  role: msg.role,
                  content: msg.content || '',
                  reasoning_content: msg.reasoning_content,
                  data: msg.data || [],
                  timestamp: msg.created_at || msg.timestamp,
                }));
                setLlmMessages(llmMsgs);
              }
            }
          } catch (error) {
            console.log('Stored session not in database');
          }
        }
      } catch (e) {
        console.error('Failed to parse stored session:', e);
      }
    }
  };
  
  restoreSession();
}, [agenticFlowId, currentProject?.id]);
```

---

### 3.8 第八阶段：对话时间显示格式优化

**文件**：`frontend/src/components/RunPanel/RunPanel.tsx`

**说明**：已在 3.6.4 中完成，复用 `formatSmartTime` 函数。

---

## 四、接口改动影响分析

### 4.1 GET /api/v1/run/sessions 接口修改

| 修改项 | 说明 |
|--------|------|
| 排序字段 | `updated_at` → `created_at` |
| 新增字段 | `first_assistant_content` |

**数据格式变化**：

```json
// 修改后
{
  "id": "xxx",
  "status": "completed",
  "created_at": "2026-03-15T10:30:00Z",
  "first_assistant_content": "您好！我是AI助手..."
}
```

---

## 五、数据流对比

### 5.1 Session创建流程

**修改前**：
```
页面加载 → 无恢复逻辑 → currentSessionId为空
```

**修改后**：
```
页面加载 → 读取localStorage中的currentSessionId → 检查数据库是否存在
  → 存在：恢复选中状态并加载消息
  → 不存在：保持新任务状态
```

### 5.2 WebSocket连接流程

**修改前**：
```
连接 → 30秒心跳 → 断开后固定间隔重连（最多5次）
```

**修改后**：
```
连接 → 15秒心跳 → 断开后指数退避重连（最多10次）
```

---

## 六、总结

| 项目 | 修改前 | 修改后 |
|------|--------|--------|
| 流式输出缩进 | 可能不一致 | 统一wordBreak |
| 工具调用折叠 | 默认折叠 | 流式时展开 |
| 面板滚动条 | 可能出现 | 优化overflow |
| 首个chunk等待 | 无反馈 | 显示加载动画 |
| WebSocket稳定性 | 30秒心跳，固定重连 | 15秒心跳，指数退避 |
| Session排序 | 按updated_at | 按created_at |
| Session名称 | 会话xxx | 第一个assistant消息 |
| Session状态 | 无显示 | 状态图标+文字 |
| Session时间 | 相对时间 | 智能时间格式（复用函数） |
| 对话时间 | 相对时间 | 智能时间格式（复用函数） |
| 新任务按钮 | "新会话" | "新任务" |
| 任务统计 | 无 | 复用sessions.length |
| currentSessionId | 不持久化 | 持久化到localStorage |

---

## 七、测试验证计划

### 7.1 测试目标

1. 验证流式输出缩进一致性
2. 验证工具调用自动展开/折叠
3. 验证面板布局无滚动条
4. 验证首个chunk等待状态显示
5. 验证WebSocket连接稳定性
6. 验证Session栏所有功能
7. 验证Session创建时机
8. 验证时间格式显示

### 7.2 测试环境

* 测试URL: http://localhost:8991

### 7.3 测试项目（每项执行3轮）

#### 测试1: 流式输出缩进测试

**测试步骤:**
1. 发送包含多级缩进的代码块消息
2. 观察流式输出过程中的缩进
3. 等待流式输出完成
4. 对比流式过程和最终显示

**记录内容（每轮）:**
* 流式过程中的缩进是否正确
* 最终显示的缩进是否正确
* 两者是否一致

**分析维度（10个）:**
1. 空格缩进是否保留
2. Tab缩进是否保留
3. 代码块格式是否正确
4. 列表格式是否正确
5. 多行文本是否正确换行
6. 中英文混排是否正常
7. 特殊字符是否正确显示
8. 长行是否正确换行
9. 行首空格是否保留
10. 行尾空格是否保留

#### 测试2: 工具调用折叠测试

**测试步骤:**
1. 发送触发工具调用的消息
2. 观察工具调用输出过程
3. 等待工具调用完成
4. 检查折叠状态

**记录内容（每轮）:**
* 工具调用过程中是否展开
* 工具调用完成后是否自动折叠
* 手动点击是否能切换状态

**分析维度（10个）:**
1. 工具调用开始时是否自动展开
2. 工具调用进行中是否保持展开
3. 工具调用完成时是否自动折叠
4. 折叠后内容是否隐藏
5. 点击标题是否能手动展开
6. 展开/折叠过渡是否平滑
7. 多个工具调用是否独立控制
8. 流式输出与静态显示是否一致
9. 展开/折叠状态是否记忆
10. 界面布局是否正确

#### 测试3: 面板布局测试

**测试步骤:**
1. 打开运行面板
2. 观察右侧是否有滚动条
3. 观察底部是否有滚动条
4. 调整窗口大小测试响应式

**记录内容（每轮）:**
* 右侧滚动条是否出现
* 底部滚动条是否出现
* 滚动条是否不必要

**分析维度（10个）:**
1. 右侧是否无滚动条
2. 底部是否无滚动条
3. 内容是否正确显示
4. 输入框是否可见
5. 消息列表是否完整
6. Session栏是否正常显示
7. 窗口缩放时布局是否正确
8. 最小化窗口时是否适配
9. 面板比例是否合理
10. 滚动是否流畅

#### 测试4: 首个chunk等待状态测试

**测试步骤:**
1. 发送一条消息
2. 立即观察界面变化
3. 等待首个chunk返回
4. 观察等待状态的消失

**记录内容（每轮）:**
* 发送后是否立即显示加载动画
* 加载动画样式是否正确
* 收到首个chunk后是否消失

**分析维度（10个）:**
1. 是否显示加载动画
2. 动画是否旋转
3. 是否显示"正在思考中"文字
4. 样式是否符合设计
5. 收到chunk后是否消失
6. 消失后是否显示内容
7. 等待时间是否合理
8. 多次发送是否正常
9. 加载失败是否处理
10. 动画是否流畅

#### 测试5: WebSocket稳定性测试

**测试步骤:**
1. 建立WebSocket连接
2. 模拟网络波动（断开网络5秒后恢复）
3. 观察重连行为
4. 发送消息验证功能

**记录内容（每轮）:**
* 断开后是否自动重连
* 重连间隔是否符合预期
* 重连后功能是否正常

**分析维度（10个）:**
1. 是否自动重连
2. 重连次数是否正确
3. 重连间隔是否递增
4. 心跳是否正常
5. 重连后消息是否丢失
6. 状态是否正确恢复
7. 最大重连次数是否正确
8. 重连失败是否有提示
9. 手动重连是否支持
10. 连接状态是否显示

#### 测试6: Session功能测试

**测试步骤:**
1. 刷新页面
2. 检查是否恢复上次选中的session
3. 创建新任务
4. 发送消息
5. 检查session名称、状态、时间显示

**记录内容（每轮）:**
* 页面刷新后session是否恢复
* 新任务创建是否正常
* session名称是否为第一个assistant消息
* 状态图标是否正确
* 时间格式是否符合智能时间规则

**分析维度（10个）:**
1. session是否按created_at倒序排列
2. 任务统计是否显示总数
3. 按钮文字是否为"新任务"
4. 状态图标颜色是否正确
5. 状态文字是否正确
6. 时间格式是否智能
7. 会话名称是否正确
8. 点击session是否切换
9. 删除session是否正常
10. 新建session是否在顶部

### 7.4 测试循环流程

```
测试 → 记录数据 → 分析（200字以上） → 发现问题 → 修复 → 重新测试
```

### 7.5 修复原则

1. 遵循现有架构设计
2. 最小化修改范围
3. 保持数据一致性
4. 不引入新问题

### 7.6 测试完成标准

1. 所有测试项执行3轮
2. 每轮数据完整记录
3. 前后端、数据库数据完全一致
4. 所有分析完成
5. 所有测试文件删除

---

## 八、检查清单

### 8.1 修改前检查

- [x] 理解所有需求点
- [x] 确认涉及文件
- [x] 了解架构约束

### 8.2 修改中检查

- [x] 每个修改有位置、代码、说明
- [x] 每个修改不影响现有功能
- [x] 每个修改遵循现有代码风格

### 8.3 修改后检查

- [ ] npm run build成功
- [ ] 后端启动成功
- [ ] 前端启动成功
- [ ] Playwright测试通过

### 8.4 测试后检查

- [ ] 所有测试项执行完成
- [ ] 所有数据完整记录
- [ ] 所有分析完成
- [ ] 无遗留问题
- [ ] 检查真实日志和返回值

---

## 九、注意事项

### 9.1 必须验证

1. 代码位置必须通过搜索验证
2. 行号必须准确
3. 调用位置必须完整
4. 修改影响必须分析
5. 修复效果必须测试验证

### 9.2 禁止行为

1. 禁止猜测代码位置
2. 禁止遗漏调用位置
3. 禁止使用模糊描述
4. 禁止不验证就记录
5. 禁止只测试不修复
6. 禁止发现问题跳过
7. 禁止预设测试成功
8. 禁止粗略查看日志
9. 禁止使用统计数据代替详细数据
10. 禁止让用户自己检查

### 9.3 质量标准

1. Plan文档可直接用于执行
2. 每个步骤都有明确的代码
3. 每个修改都有原因说明
4. 测试验证计划完整可执行

---

## 十、执行检查口诀

```
一搜二读三定位
四修五测六验证
七记八析九总结
全部通过才算完
```
