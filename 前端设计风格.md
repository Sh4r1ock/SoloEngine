# SoloEngine 前端设计风格

## 设计理念

SoloEngine 采用现代简约的设计风格，强调清晰的信息层级和流畅的用户体验。整体设计遵循以下原则：

- **简洁性**: 去除不必要的装饰，突出核心功能
- **一致性**: 统一的视觉语言和交互模式
- **专业性**: 适合开发者工具的专业外观
- **高效性**: 快速响应，流畅动画
- **层次感**: 通过阴影、间距、色彩对比度建立清晰的视觉层次

## 配色方案

### 主色调

```css
--primary-color: #2563eb;
--primary-hover: #3b82f6;
--primary-active: #1d4ed8;
```

### 状态色

```css
--success-color: #10b981;
--success-hover: #34d399;

--warning-color: #f59e0b;
--warning-hover: #fbbf24;

--error-color: #ef4444;
--error-hover: #f87171;
```

### 中性色

```css
--bg-primary: #ffffff;
--bg-secondary: #f8f9fa;
--bg-tertiary: #f1f5f9;
--bg-elevated: #ffffff;
--bg-surface: #f0f2f5;

--text-primary: #0f172a;
--text-secondary: #475569;
--text-tertiary: #6b7280;
--text-quaternary: #9ca3af;
--text-disabled: #9ca3af;

--border-color: #e2e8f0;
--border-color-light: #f1f5f9;
--border-color-lighter: #f8f9fa;
```

### 阴影系统

```css
--shadow-xs: 0 1px 2px 0 rgba(0, 0, 0, 0.05);
--shadow-sm: 0 2px 8px rgba(0, 0, 0, 0.08);
--shadow-base: 0 4px 12px rgba(0, 0, 0, 0.12);
--shadow-lg: 0 8px 24px rgba(0, 0, 0, 0.15);
--shadow-xl: 0 12px 32px rgba(0, 0, 0, 0.18);
```

## 字体系统

### 字体族

```css
--font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Roboto', 'Oxygen',
  'Ubuntu', 'Cantarell', 'Fira Sans', 'Droid Sans', 'Helvetica Neue', sans-serif;
--font-family-code: 'SF Mono', Monaco, 'Cascadia Code', 'Roboto Mono', Consolas,
  'Courier New', monospace;
```

### 字体大小

```css
--font-size-xs: 12px;
--font-size-sm: 13px;
--font-size-base: 14px;
--font-size-lg: 16px;
--font-size-xl: 18px;
--font-size-xxl: 24px;
```

### 字重

```css
--font-weight-normal: 400;
--font-weight-medium: 500;
--font-weight-semibold: 600;
--font-weight-bold: 700;
```

## 间距系统

使用 4px 基础单位，统一使用 8px 作为主要间距：

```css
--spacing-xs: 4px;
--spacing-sm: 8px;
--spacing-md: 16px;
--spacing-lg: 24px;
--spacing-xl: 32px;
--spacing-xxl: 48px;
```

## 圆角

```css
--radius-sm: 4px;
--radius-base: 8px;
--radius-lg: 12px;
--radius-xl: 16px;
--radius-xxl: 24px;
```

## 动画

### 过渡时间

```css
--duration-instant: 0.1s;
--duration-fast: 0.2s;
--duration-base: 0.3s;
--duration-slow: 0.5s;
```

### 缓动函数

```css
--ease-in-out: cubic-bezier(0.645, 0.045, 0.355, 1);
--ease-out: cubic-bezier(0.215, 0.61, 0.355, 1);
--ease-in: cubic-bezier(0.55, 0.055, 0.675, 0.19);
```

## 组件设计规范

### 头部

- 高度: 64px
- 背景色: #ffffff
- 边框: 1px solid #f1f5f9
- 阴影: var(--shadow-sm)
- 内边距: 0 24px
- 品牌文字: #2563eb, 20px, 700 weight, 0.5 letter-spacing
- 按钮：运行按钮（绿色 #10b981）、新建项目按钮

### 工具栏

- 位置: 固定在底部中央
- 背景色: #ffffff
- 圆角: 8px
- 阴影: var(--shadow-base)
- 边框: 1px solid #f1f5f9
- 内边距: 8px
- 按钮间距: 8px
- 悬浮效果: translateY(-1px) + 增强阴影
- 按钮：添加 Agent、放大、缩小、设置

### 按钮

- 高度: 36px
- 圆角: 8px
- 内边距: 0 16px
- 过渡: 0.2s ease-in-out
- 字重: 500 (medium)
- 悬浮: 阴影增强 + translateY(-1px)
- 点击: 阴影减弱 + translateY(0)

### 运行按钮

- 位置: 头部右侧
- 背景色: #10b981 (success)
- 边框色: #10b981
- 高度: 36px
- 字重: 600 (semibold)
- 悬浮: 背景色加深

### 输入框

- 高度: 36px
- 圆角: 8px
- 边框: 1px solid #e2e8f0
- 聚焦边框: 1px solid #2563eb
- 聚焦阴影: 0 0 0 3px rgba(37, 99, 235, 0.1)

### 节点卡片

- 宽度: 220px
- 圆角: 8px
- 边框: 1px solid #e2e8f0
- 选中边框: 2px solid #2563eb + 蓝色光晕
- 内边距: 12px
- 默认阴影: var(--shadow-sm)
- 悬浮阴影: var(--shadow-base) + translateY(-2px)
- 拖拽时: 阴影增强 + cursor: grabbing
- 内容：节点名称、模型名称、节点简介

### 右侧属性面板

- 宽度: 320px
- 位置: 固定在右侧
- 背景色: #ffffff
- 边框: 1px solid #f1f5f9
- 阴影: -2px 0 8px rgba(0, 0, 0, 0.06)
- 头部：节点属性标题 + 关闭按钮
- 内容：节点名称、节点简介、Agent 类型、模型配置、提示词配置、技能绑定
- 提示词字段：System Prompt、User Prompt、Assistant Prompt（可隐藏）

### 模态框

- 圆角: 12px
- 阴影: var(--shadow-xl)
- 内边距: 24px
- 头部边框: 1px solid #f1f5f9
- 底部边框: 1px solid #f1f5f9

### 画布

- 背景色: #f8f9fa
- 网格点: #e2e8f0
- 网格间距: 20px

## 视觉层次

### 层级 1: 最高优先级
- 运行按钮 (绿色 #10b981)
- 错误提示 (红色 #ef4444)
- 节点选中状态 (蓝色光晕)

### 层级 2: 次要操作
- 主要按钮 (蓝色 #2563eb)
- 工具栏按钮
- 模态框标题

### 层级 3: 常规内容
- 输入框
- 下拉菜单
- 节点卡片
- 文字内容

### 层级 4: 背景元素
- 画布背景
- 模态框背景
- 工具栏背景

### 层级 5: 装饰元素
- 分隔线
- 辅助文字
- 图标

## 交互规范

### 悬浮状态
- 按钮: 阴影增强 + 向上移动 1px
- 卡片: 阴影增强 + 向上移动 2px
- 节点: 阴影增强 + 向上移动 2px
- 边框: 颜色加深

### 点击状态
- 按钮: 阴影减弱 + 回到原位
- 节点: 选中高亮 + 蓝色光晕
- 输入框: 蓝色边框 + 蓝色阴影

### 拖拽状态
- 节点: cursor: grabbing + 阴影增强
- 拖拽目标: cursor: crosshair + 半透明遮罩

### 禁用状态
- 背景色: #f1f5f9
- 文字色: #9ca3af
- 边框色: #e2e8f0

## 色彩搭配原则

### 现代化配色
- 主色调使用鲜艳的蓝色 (#2563eb)
- 状态色使用鲜艳的绿色、橙色、红色
- 背景色使用柔和的灰色系 (#f8f9fa, #f1f5f9)
- 文字色使用更深的灰色系 (#0f172a, #475569)
- 避免使用低饱和度的颜色

### 单色原则
- 所有颜色使用纯色，不使用渐变效果
- 避免过度饱和的色彩
- 保持色彩的一致性和专业性

### 对比度
- 文字与背景对比度不低于 4.5:1
- 边框与背景对比度适中
- 状态色与背景有足够对比

### 辅助色
- 使用灰色系作为辅助色
- 保持主色调的突出
- 避免过多颜色造成视觉混乱

## 图标规范

- 使用 Ant Design Icons
- 尺寸: 16px (小), 20px (中), 24px (大)
- 颜色: 继承父元素文字颜色
- 按钮内图标: 白色

## 响应式断点

```css
--screen-xs: 480px;
--screen-sm: 576px;
--screen-md: 768px;
--screen-lg: 992px;
--screen-xl: 1200px;
--screen-xxl: 1600px;
```

## 特殊说明

1. **禁止使用渐变色**: 所有颜色使用纯色，不使用渐变效果
2. **统一圆角**: 所有组件统一使用 8px 圆角（除非特殊说明）
3. **简洁设计**: 避免过度装饰，保持界面清爽
4. **性能优先**: 动画和过渡效果要流畅，不影响性能
5. **视觉层次**: 通过阴影、间距、色彩对比度建立清晰的视觉层次
6. **统一按钮风格**: 所有按钮使用相同的设计语言（圆角、阴影、过渡）
7. **现代化配色**: 使用更鲜艳、更现代的颜色方案

## 代码示例

### 头部样式

```css
.header {
  height: 64px;
  background: #ffffff;
  border-bottom: 1px solid #f1f5f9;
  box-shadow: var(--shadow-sm);
  padding: 0 24px;
  display: flex;
  align-items: center;
  justify-content: space-between;
}
```

### 工具栏样式

```css
.toolbar {
  position: fixed;
  bottom: 24px;
  left: 50%;
  transform: translateX(-50%);
  display: flex;
  gap: 8px;
  padding: 8px;
  background: #ffffff;
  border-radius: 8px;
  box-shadow: var(--shadow-base);
  border: 1px solid #f1f5f9;
}
```

### 按钮样式

```css
.button {
  height: 36px;
  padding: 0 16px;
  border-radius: 8px;
  background: var(--primary-color);
  color: white;
  border: none;
  cursor: pointer;
  transition: all 0.2s ease-in-out;
  box-shadow: var(--shadow-xs);
  font-weight: 500;
}

.button:hover {
  box-shadow: var(--shadow-sm);
  transform: translateY(-1px);
}

.button:active {
  transform: translateY(0);
  box-shadow: var(--shadow-xs);
}
```

### 输入框样式

```css
.input {
  height: 36px;
  padding: 0 8px;
  border-radius: 8px;
  border: 1px solid var(--border-color);
  background: var(--bg-primary);
  color: var(--text-primary);
  transition: all 0.2s ease-in-out;
}

.input:focus {
  border-color: var(--primary-color);
  outline: none;
  box-shadow: 0 0 0 3px rgba(37, 99, 235, 0.1);
}
```

### 节点卡片样式

```css
.node-card {
  width: 220px;
  border-radius: 8px;
  border: 1px solid var(--border-color);
  padding: 12px;
  background: var(--bg-primary);
  box-shadow: var(--shadow-sm);
  transition: all 0.2s ease-in-out;
}

.node-card:hover {
  box-shadow: var(--shadow-base);
  transform: translateY(-2px);
}

.node-card.selected {
  border: 2px solid var(--primary-color);
  box-shadow: 0 0 0 4px rgba(37, 99, 235, 0.3);
}
```

### 右侧面板样式

```css
.property-panel {
  width: 320px;
  height: 100%;
  padding: 24px;
  border-left: 1px solid #f1f5f9;
  background: #ffffff;
  overflow-y: auto;
  box-shadow: -2px 0 8px rgba(0, 0, 0, 0.06);
}
```

## 更新日志

- 2026-02-03: 初始版本，定义 SoloEngine 前端设计风格
- 2026-02-03: 完整重构，增强视觉层次和设计感
- 2026-02-03: 优化节点卡片和属性面板，更新为现代化配色方案
- 2026-02-03: 移除设置按钮，修复节点点击事件，更新颜色方案为更现代化的配色
